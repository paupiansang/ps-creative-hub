async function api(u){const r=await fetch(u);const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Login required');return d}
function esc(s){return String(s||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
async function run(){const d=await api('/api/library');const el=document.querySelector('#library');el.innerHTML=d.products.map(p=>`<article class="product"><div class="product-thumb">${p.thumbnail?`<img src="${p.thumbnail}" alt="${esc(p.title)}">`:'<div class="thumb-mark">PS</div>'}</div><div class="product-body"><span class="tag">Owned</span><h3>${esc(p.title)}</h3><p>${esc(p.description||'')}</p><div class="card-row"><span class="muted">Unlocked</span><button class="primary-btn" onclick="download(${p.id})">Download</button></div></div></article>`).join('')||'<div class="empty-state"><h3>Your library is empty</h3><p class="muted">Explore the store and unlock your first asset.</p><a class="primary-btn" href="/#store">Explore Store</a></div>'}
async function download(id){const r=await fetch('/api/products/'+id+'/download',{redirect:'manual'});if(r.ok){window.location.href='/api/products/'+id+'/download';return}const d=await r.json().catch(()=>({}));alert(d.error||'Download unavailable.')}
run().catch(e=>document.querySelector('#library').innerHTML=`<div class="empty-state"><h3>${e.message}</h3><p class="muted">Login from the home page to access your library.</p><a class="primary-btn" href="/?login=1">Go to Login</a></div>`);
